# notebooks

Analysis notebooks used to generate report-ready results and visualizations.

## Main Notebook

`thuggerdaily_trial_effect_report.ipynb`

This notebook loads the real processed dashboard data, combines the local RTF timeline with web-verified trial dates, estimates ThuggerDaily-aligned event-window shifts, and writes report-ready figures to `reports/figures/`.

